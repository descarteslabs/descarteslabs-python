language: python
python:
  - "2.6"
  - "2.7"
  - "3.3"
  - "3.4"
  - "3.5"
  - "3.6"
  - "3.7-dev"

install:
 - pip install .
 - pip install -r requirements.txt
 - pip install flake8 coverage nose

script:
 - nosetests --with-coverage --cover-package=descarteslabs
 - flake8
